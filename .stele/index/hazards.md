# Hazards

| claim | node | anchor |
| --- | --- | --- |
| macOS /bin/bash is frozen at 3.2.57 — no mapfile, declare -A, ${var,,}, negative substrings, or wait -n anywhere in this script | bin | lm:bash-3-2 |
